# 快速开始指南

## 5 分钟上手

### 第一步：安装依赖

```bash
cd frontend
npm install
```

⏱️ 大约需要 2-3 分钟

### 第二步：启动后端服务

```bash
# 在项目根目录
cd ..
source .venv/bin/activate
uvicorn main:app --reload --port 8080
```

### 第三步：选择运行模式

#### 方式一：浏览器模式（推荐新手）

```bash
cd frontend
npm run dev
```

打开浏览器访问：http://localhost:5173

#### 方式二：桌面应用模式

```bash
cd frontend
npm run electron:dev
```

会自动打开桌面应用窗口

## 默认登录信息

- **用户名**：（根据数据库配置）
- **密码**：（根据数据库配置）

## 验证是否成功

✅ 能看到登录页面  
✅ 能成功登录  
✅ 能看到主界面（就诊管理、患者管理等菜单）  

## 遇到问题？

### 问题：端口被占用

**错误提示**：`Port 5173 is already in use`

**解决**：
```bash
# 使用其他端口
npm run dev -- --port 5174
```

### 问题：无法连接后端

**错误提示**：`Network Error` 或 `ERR_CONNECTION_REFUSED`

**检查**：
1. 后端是否已启动？运行：`curl http://localhost:8080/api/health`
2. 端口是否正确？检查 `.env.local` 文件
3. 防火墙是否阻止？临时关闭防火墙测试

### 问题：Electron 白屏

**解决**：
```bash
# 确保 Vite 开发服务器已启动
npm run dev

# 在另一个终端启动 Electron
npm run electron:dev
```

### 问题：npm install 失败

**解决**：
```bash
# 使用国内镜像
npm config set registry https://registry.npmmirror.com
npm install

# 或使用 cnpm
npm install -g cnpm --registry=https://registry.npmmirror.com
cnpm install
```

## 下一步

- 📖 阅读 [README.md](./README.md) 了解项目结构
- 🚀 阅读 [ELECTRON.md](./ELECTRON.md) 学习打包桌面应用
- ⚙️ 阅读 [CONFIG.md](./CONFIG.md) 配置环境变量
- 📦 阅读 [INSTALL.md](./INSTALL.md) 详细安装说明

## 开发提示

### 热更新

修改代码后自动刷新，无需重启服务器

### 开发者工具

- **浏览器模式**：按 `F12` 打开
- **Electron 模式**：按 `Ctrl+Shift+I` (Windows/Linux) 或 `Cmd+Option+I` (macOS)

### 推荐插件

在 VS Code 中安装：
- Volar (Vue 3)
- ESLint
- Prettier

## 常用命令

```bash
# Web 模式
npm run dev              # 开发环境
npm run build            # 生产构建
npm run preview          # 预览构建

# Electron 模式
npm run electron:dev     # 开发环境
npm run electron:build   # 打包应用

# 其他
npm run lint             # 代码检查（如果配置了）
npm run format           # 代码格式化（如果配置了）
```

## 项目目录快速导航

```
frontend/
├── src/
│   ├── views/          # 页面组件（从这里开始）
│   ├── components/     # 可复用组件
│   ├── api/           # API 接口定义
│   ├── router/        # 路由配置
│   └── store/         # 状态管理
├── electron/          # Electron 相关代码
└── public/            # 静态资源
```

## 技术栈

- **前端框架**：Vue 3
- **构建工具**：Vite
- **桌面框架**：Electron
- **UI 组件**：Ant Design Vue + Element Plus
- **状态管理**：Pinia
- **路由**：Vue Router

祝开发愉快！🎉

